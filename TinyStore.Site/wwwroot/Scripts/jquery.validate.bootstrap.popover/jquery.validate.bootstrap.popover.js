// Generated by CoffeeScript 1.6.3
// fixed for bootstrap 4
(function () {
    var $;

    $ = jQuery;

    $.fn.extend({
        validate_popover: function (options) {
            var settings = $.extend(true, {}, $.validator.popover_defaults, options);
            if (settings.get_offset_element) {
                $.validator.get_offset_element = settings.get_offset_element;
            }
            return this.validate(settings);
        }
    });

    $.extend($.validator, {
        popover_defaults: {
            onsubmit: true,
            popoverPosition: 'top',
            popoverContainer: 'body',
            hideForInvisible: true,
            success: function (error, element) {
                return $(element).popover('hide');
                //return $.validator.hide_validate_popover(element);
            },
            beforeShowError: function (message) { },
            errorPlacement: function (error, element) {
                var setting = {
                    container: $.validator.validator_settings(element).popoverContainer,
                    placement: $.validator.validator_settings(element).popoverPosition,
                    trigger: 'manual',
                    content: error.html(),
                    template: '<div class="popover border-danger" role="tooltip"><div class="arrow"></div><h3 class="popover-header text-danger font-weight-bolder"></h3><div class="popover-body text-danger font-weight-bold font-italic"></div></div>'
                };
                $(element).popover(setting);
                return $(element).popover('show');
            },
        },
        validator_settings: function (element) {
            return $.data($(element)[0].form, "validator").settings;
        }
    });

}).call(this);
